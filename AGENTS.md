# StudioOS-CEAL Agent Contract

本文件对整个仓库生效。代理进入仓库后必须自动读取并执行，无需用户在每个提示词里重复交付要求。

## 1. 默认语言与目标

- 用户可见说明、计划、报告默认使用简体中文；代码标识符保持英文。
- 把每次修改视为一笔受控工程事务：Contract → Baseline → Implement → Test → Evidence → Audit → Close。
- 只交付真实完成且有当前证据支持的结果。文件存在、路由存在、HTTP 200、mock、历史 PASS 均不能单独证明功能完成。

## 2. 开始任务前

1. 读取本文件、用户请求、适用的 \`TASK_PACKAGE\`、当前状态和最近实施报告。
2. 判断任务模式：\`answer\`、\`diagnose\`、\`implement_and_verify\`、\`read_only_audit\`。
3. 修改前记录 Git 分支、HEAD、工作区状态以及目标文件基线。
4. 检查 allowlist、denylist、风险等级、验收命令、回滚和停止条件。
5. 已有 \`APPROVED_FOR_IMPLEMENTATION\` 且动作在 allowlist 内时，直接实施，不重复询问批准。

## 3. 必须停止并询问的情况

仅在下列实质性阻塞出现时暂停：

- 所需文件或动作不在 allowlist，或命中 denylist；
- 需要新增权限、凭据、付费服务、外部发信/发布、生产写入或扩大任务范围；
- 需要破坏性、不可逆或难以恢复的操作；
- 现有证据互相矛盾，或继续测试会污染生产状态；
- 用户选择会实质改变架构、数据迁移或风险；
- 无法在隔离环境安全验证写操作。

普通可逆编辑、已有授权范围内的测试和修复不得重复向用户确认。

## 4. 实施规则

- 保留用户已有修改；不得擅自清理脏工作区。
- 优先复用现有实现；禁止创建 \`new\`、\`v2\`、\`legacy\` 等平行系统逃避整合。
- 只修改最小必要文件；范围变化必须先提交 Change Request。
- 禁止伪造日志、测试、hash、运行时间、截图、API 结果或 8000 次执行声明。
- 禁止写入真实 Secret；示例只能使用占位符。
- 禁止自动付费、下载大型模型、公开监听、修改生产队列或生产数据。
- 写数据测试必须使用临时目录、临时数据库或隔离端口。
- 不得使用 \`git reset --hard\`、无明确目标的递归删除或其他高风险恢复命令。

## 5. 验证与状态

- 源码修改必须有直接相关自动测试；产品功能还必须有适当的集成或运行证据。
- 保存真实命令、退出码、关键输出和产物 SHA-256。
- 状态只允许基于证据逐级声明：
  - \`PLANNED\`
  - \`IMPLEMENTING\`
  - \`CODE_COMPLETE\`
  - \`TEST_PASS\`
  - \`RUNTIME_VERIFIED\`
  - \`PRODUCTION_VERIFIED\`
  - \`BLOCKED\`
- 低级状态不得暗示高级状态。CEAL 自身测试通过不能证明 StudioOS 或任何外部 Runtime READY。
- “8000+ 对抗验证”只有生成器确认 8000 个唯一组合、oracle mismatch 为 0、至少两轮零新增 P0/P1 且报告和 hash 已落盘时才能声明。

## 6. Git 与交付

- 默认在独立分支提交并创建 PR；除非用户明确要求，否则不直接写主分支。
- 每个 Sprint 必须提供清晰的允许/禁止文件、完成标准、测试、风险、回滚和 \`IMPLEMENTATION_REPORT\`。
- 最终回复固定顺序：
  1. 实际结果与当前状态；
  2. 修改文件/提交/PR；
  3. 执行过的测试与退出码；
  4. 证据和 hash；
  5. 未完成项、风险或 blocker；
  6. 回滚方式；
  7. 唯一必要的下一步。
- 若任务已全部完成，不要为了延长流程而要求用户再次输入“继续”。

## 7. 用户以后最短提示词

用户可以只写：

\`\`\`text
执行 TASK-XXX：<目标>。
按仓库 AGENTS.md 自动完成；在已授权范围内不要重复询问。
\`\`\`

如任务涉及产品代码，再附一个任务包路径或明确 allowlist。其余交付结构由本文件自动约束。
