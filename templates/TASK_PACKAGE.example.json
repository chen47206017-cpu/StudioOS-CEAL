{
  "schema_version": "1.0",
  "task_id": "TASK-001",
  "title": "Describe one bounded engineering outcome",
  "mode": "implement_and_verify",
  "status": "APPROVED_FOR_IMPLEMENTATION",
  "risk_level": "L1",
  "scope": {
    "allow": [
      "src/**",
      "tests/**",
      "docs/**"
    ],
    "deny": [
      ".env",
      "production-data/**",
      "secrets/**"
    ]
  },
  "changed_paths": [],
  "acceptance": {
    "required_commands": [
      "python -m unittest discover -s tests -v"
    ],
    "runtime_evidence_required": false,
    "production_evidence_required": false
  },
  "rollback": {
    "strategy": "Revert only the task commit or restore the listed files from the recorded baseline."
  },
  "stop_conditions": [
    "A required change falls outside scope.allow.",
    "A requested action touches scope.deny.",
    "New credentials, paid services, destructive actions, or production writes are required.",
    "Evidence contradicts a PASS claim."
  ]
}
